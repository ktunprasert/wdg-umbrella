<h1><%= @page.title %></h1>
<p>Posted on <%= @page.date %>
<%= unless Enum.empty?(@page.tags) do %>
  <p>Tags:</p>
  <ul>
    <%= for t <- @page.tags do %>
      <li><a href="<%= t.list_url %>"><%= t.name %></a></li>
    <% end %>
  </ul>
<% end %>

<% dbg(@page) %>
<%= @contents %>
